# Notification signals - hooks to boarding events
# This file is imported in apps.py ready() method

import logging

logger = logging.getLogger(__name__)

# Signal handlers will be added here
# The boarding event hook is in realtime/signals.py to avoid circular imports
